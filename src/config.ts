import type { Locale } from '@/i18n'

// Blog configuration
export const config = {
  // Default language for the blog
  // Set to 'en' for English, 'zh-CN' for Chinese
  DEFAULT_LOCALE: 'en' as Locale,
}
